import 'dart:ui';

import 'package:flutter/material.dart';

/// Define your app-wide colors for light and dark themes
class AppColors {
  // Primary color
  static const Color primary = Color(0xFF8E6CEF);

  // Light theme colors
  static const Color lightBackground = Color(0xFFF5F5F5);
  static const Color lightScaffold = Color(0xFFFFFFFF);
  static const Color lightSecondaryBackground = Color(0xFFE0E0E0);
  static const Color lightText = Color(0xFF000000);
  static const Color lightHint = Color(0xFF9E9E9E);

  // Dark theme colors
  static const Color darkBackground = Color(0xFF1D182A);
  static const Color darkScaffold = Color(0xFF342F3F);
  static const Color darkSecondaryBackground = Color(0xFF2C2535);
  static const Color darkText = Colors.white;
  static const Color darkHint = Color(0xFFA7A7A7);

  // Shared
  static const Color white = Colors.white;
}
